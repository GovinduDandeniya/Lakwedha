import 'package:flutter/material.dart';

class AppColors {
    AppColors._();

  // Primary palette — Premium Deep Forest Green
  static const Color primary = Color(0xFF1B5E20);       // Deep forest green
  static const Color primaryDark = Color(0xFF0D3B13);   // Very dark green
  static const Color accent = Color(0xFF2E7D32);        // Medium green — CTA / interactive
  static const Color accentLight = Color(0xFF4CAF50);   // Light green — highlights & icons

  // Background
  static const Color backgroundBlur = Color(0xCCF5FAF5); // Frosted greenish-white card (80% opacity)
  static const Color textFieldBg = Color(0xFFF0F7F0);    // Very light green input background

  // Text
  static const Color textDark = Color(0xFF1A1A1A);
  static const Color textMedium = Color(0xFF555555);
  static const Color textLight = Color(0xFF888888);
  static const Color textWhite = Color(0xFFFFFFFF);

  // Status
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);

  // Border
  static const Color border = Color(0xFFCEDECE);        // Soft green border
}