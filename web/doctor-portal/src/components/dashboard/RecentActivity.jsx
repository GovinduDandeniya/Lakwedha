import React from 'react';
import { Box, Typography } from '@mui/material';

const RecentActivity = () => (
    <Box><Typography>No recent activity.</Typography></Box>
);

export default RecentActivity;
